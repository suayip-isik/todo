import { StyleSheet } from "react-native";

export default StyleSheet.create({
    container: {
        backgroundColor: 'gray',//'rgba(25, 200, 25, 1)',
        color: 'white',
        padding: 15,
        borderRadius: 20,
        marginRight: '3%',
        alignItems: 'center',
        justifyContent: 'center',
    },
});