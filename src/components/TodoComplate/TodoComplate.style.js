import { StyleSheet } from "react-native";

export default StyleSheet.create({
    container: {
        backgroundColor: 'rgba(25, 200, 25, 1)',
        color: 'white',
        padding: 20,
        borderRadius: 20,
        marginRight: '3%',
        alignItems: 'center',
        justifyContent: 'center',
    },
});